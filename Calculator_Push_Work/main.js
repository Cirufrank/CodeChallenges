const calculatorBody = document.querySelector()


function makeCalculator(fontSize, textColor, mainColor) {
    this.fontSize = fontSize;
    this.textColor = textColor;
    this.mainColor = mainColor;
    this.makeCalculation = function() {
        /* store input and return result */
        console.log("calculation");
    }
    this.displayCalculation = function() {
        /* Display input once it has been stored */
        console.log("display caclulation");
    }
    this.displayResult = function() {
        /* Display result */
        console.log("Display Result")
    }
    this.setFontColor = function() {

    }
}